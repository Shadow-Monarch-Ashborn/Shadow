# Simple PHP Website

This repository contains a tiny, single-page PHP website.

## Run locally

If PHP is installed, you can run:

```bash
php -S localhost:8000
```

Then open <http://localhost:8000/index.php>.
